version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/spring-cafe-deploy/
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: ec2-user
